{
  "name": "linklocker",
  "version": "1.0.0",
  "private": true,
  "description": "LinkLocker monorepo",
  "scripts": {
    "dev:web": "cd apps/web && npm run dev",
    "dev:cli": "node apps/cli/index.js",
    "setup": "npm install && cd apps/web && npm install",
    "build": "turbo run build",
    "test": "cd packages/bookmark-manager && node test.js"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "turbo": "^2.9.5"
  }
}
